const extCi = require('@cloudbase/extension-ci')
const defaultTcb = require('./tcb')

module.exports = {
    name: extCi.name,
    invoke: async function (options, tcb) {
        tcb = tcb || defaultTcb
        return await extCi.invoke(options, tcb)
    }
}